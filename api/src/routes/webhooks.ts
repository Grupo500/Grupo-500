import { Router } from 'express'

const router = Router()

// Webhooks de Clerk eliminados — autenticación migrada a NextAuth
// Este archivo se mantiene por si se agregan webhooks de otros servicios

export default router
